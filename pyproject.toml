[project]
name = "alt-text"
version = "0.1.0"
description = "AI-assisted alt text pipeline for archival image collections, with calibration, HTML review, and multi-provider support"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.111.0",
    "google-genai>=2.9.0",
    "openai>=2.43.0",
    "pillow>=12.2.0",
    "portkey-ai>=2.3.2",
    "tenacity>=9.1.4",
]
